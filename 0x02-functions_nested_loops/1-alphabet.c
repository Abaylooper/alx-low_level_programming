#include "main.h"

/**
 * print_alphabet -Writes the lower case alphabets
 * Return: On Success 0
 */

void print_alphabet(void)


{
char c;
	for (c = 'a'; c <= 'z'; c++)
		_putchar(c);
_putchar('\n');
}
